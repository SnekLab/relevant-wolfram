module.exports = {
    BASE_URL: 'http://api.wolframalpha.com',
    POD: class Pod {
       constructor({}) {
          return new Error('to be done later luke needed this fast');
       }
    },
    RESPONSE: class Response {
       constructor({}) {
          return new Error('to be done later luke needed this fast');
       }
    }
};
